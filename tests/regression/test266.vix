fn double_all(arr: [i32]) {
    for (i in 0 .. arr.length) {
        arr[i] = arr[i] * 2
    }
}
fn main(): i32 {
    let arr = [1, 2, 3, 4, 5]
    double_all(arr)
    for (v in arr) {
        print(v)
    }
    return 0
}
