fn clamp(x: i32, lo: i32, hi: i32): i32
{
	if (x < lo) {
		return lo
	}
	if (x > hi) {
		return hi
	}
	return x
}

fn main(): i32
{
	print(clamp(5, 0, 10))
	print(clamp(-5, 0, 10))
	print(clamp(15, 0, 10))
	print(clamp(0, 0, 10))
	print(clamp(10, 0, 10))
	return 0
}
