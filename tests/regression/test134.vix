fn find_max(arr: [i32], size: i32) -> i32
{
	let max_val = arr[0]
	for (i in 1 .. size) {
		if (arr[i] > max_val) {
			max_val = arr[i]
		}
	}
	return max_val
}

fn main(): i32
{
	let arr = [3, 7, 2, 9, 1, 5]
	print(find_max(arr, 6))
	let arr2 = [100]
	print(find_max(arr2, 1))
	return 0
}
