fn sum(arr: [i32]): i32
{
    let mut s = 0
    let len = arr.length
    for (i in 0..length)
    {
        s = s + arr[i]
    }
    return s
}

fn main(): i32
{
    let a = [10, 20, 30]
    let b = [100, 200]

    print(sum(a))
    print(sum(b))
    print(sum([1, 2, 3, 4, 5]))

    return 0
}
