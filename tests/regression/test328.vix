fn digit_sum(n: i32): i32 {
    let mut sum = 0
    let mut x = n
    while (x > 0) {
        sum = sum + x % 10
        x = x / 10
    }
    return sum
}
fn main(): i32 {
    print(digit_sum(123))
    print(digit_sum(9999))
    print(digit_sum(100))
    return 0
}
