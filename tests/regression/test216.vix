// Recursive fibonacci
fn fib(n: i32) -> i32
{
	if (n <= 1) {
		return n
	}
	return fib(n - 1) + fib(n - 2)
}

fn main(): i32
{
	print(fib(0))
	print(fib(1))
	print(fib(5))
	print(fib(10))
	print(fib(15))
	return 0
}
