fn gcd(a: i32, b: i32): i32
{
	while (b != 0) {
		let t = b
		b = a % b
		a = t
	}
	return a
}

fn lcm(a: i32, b: i32): i32
{
	return a / gcd(a, b) * b
}

fn main(): i32
{
	print(gcd(12, 8))
	print(gcd(100, 75))
	print(lcm(4, 6))
	print(lcm(3, 7))
	return 0
}
