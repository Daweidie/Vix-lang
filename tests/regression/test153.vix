fn main(): i32
{
	let a = Ok(42) : Result<i32, string>
	match a {
		Ok(v) -> { print(v) }
		Err(e) -> { print(e) }
	}
	let b = Err("oops") : Result<i32, string>
	match b {
		Ok(v) -> { print(v) }
		Err(e) -> { print(e) }
	}
	return 0
}
