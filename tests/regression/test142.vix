fn digit_sum(n: i32) -> i32
{
	let mut sum = 0
	let mut val = n
	while (val > 0) {
		sum = sum + val % 10
		val = val / 10
	}
	return sum
}

fn main(): i32
{
	print(digit_sum(0))
	print(digit_sum(1))
	print(digit_sum(123))
	print(digit_sum(9999))
	return 0
}
