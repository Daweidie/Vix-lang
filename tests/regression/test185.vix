fn main(): i32
{
	let x = 10
	let mut y = 0
	while (x > 0) {
		if (x == 5) {
			x = x - 1
			continue
		}
		y = y + x
		x = x - 1
	}
	print(y)

	let mut i = 0
	let mut sum = 0
	while (i < 10) {
		i = i + 1
		if (i > 5) {
			break
		}
		sum = sum + i
	}
	print(sum)
	print(i)
	return 0
}
