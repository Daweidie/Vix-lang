fn main(): i32 {
    let arr = [10, 20, 30]
    let idx = 1
    if (idx >= 0 and idx < arr.length) {
        print(arr[idx])
    } else {
        print(-1)
    }
    let idx2 = 5
    if (idx2 >= 0 and idx2 < arr.length) {
        print(arr[idx2])
    } else {
        print(-1)
    }
    return 0
}
