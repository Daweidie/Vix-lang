fn main(): i32 {
    let s = "hello"
    for (i in 0 .. s.length) {
        let c: i32 = s[i]
        if (c >= 97 and c <= 122) {
            print(c - 32)
        } else {
            print(c)
        }
    }
    return 0
}
