type Result<T, E> = Ok(T) | Err(E)

fn main(): i32 {
    let bad = Err("boom"): Result<i32, string>
    
    match bad {
        Ok(v) -> print("matched-ok")
        Err(e) -> print("matched-err")
    }
    
    return 0
}