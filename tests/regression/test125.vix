fn fact_iter(n: i32) -> i32
{
	let mut result = 1
	let mut i = 2
	while (i <= n) {
		result = result * i
		i = i + 1
	}
	return result
}

fn main(): i32
{
	print(fact_iter(0))
	print(fact_iter(1))
	print(fact_iter(5))
	print(fact_iter(10))
	return 0
}
