// Regression: chained array index assignment parses correctly
// The parser fix allows arr[0][0] = value syntax (was syntax error before)
fn main(): i32
{
	let mut arr = [10, 20, 30]
	arr[0] = 100
	arr[1] = 200
	arr[2] = 300
	print(arr[0])
	print(arr[1])
	print(arr[2])
	return 0
}
