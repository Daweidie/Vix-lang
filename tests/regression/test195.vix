type Result:[T, E] = Ok(T) | Err(E)

fn main(): i32
{
	let r1: Result[i32, i32] = Ok(42)
	let r2: Result[i32, i32] = Err(-1)

	match r1 {
		Ok(v) -> print(v)
		Err(e) -> print(e)
	}

	match r2 {
		Ok(v) -> print(v)
		Err(e) -> print(e)
	}
	return 0
}
