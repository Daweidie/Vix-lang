type Node = struct { val: i32, next: &Node }
fn main(): i32 {
    let c = Node{ val: 3, next: nil }
    let b = Node{ val: 2, next: &c }
    let a = Node{ val: 1, next: &b }
    print(a.val)
    print(a.next.val)
    print(a.next.next.val)
    return 0
}
