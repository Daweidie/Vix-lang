fn apply(f: fn(i32): i32, x: i32): i32
{
	return f(x)
}

fn double_val(x: i32): i32 { return x * 2 }
fn add_one(x: i32): i32 { return x + 1 }

fn main(): i32
{
	print(apply(double_val, 5))
	print(apply(add_one, 10))
	let f = fn(n: i32): i32 { n * 3 }
	print(f(4))
	return 0
}
