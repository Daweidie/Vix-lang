// Boolean logic with and/or keywords
fn check(x: i32): i32
{
	if (x > 0 and x < 100) {
		return 1
	}
	if (x <= 0 or x >= 100) {
		return -1
	}
	return 0
}

fn main(): i32
{
	print(check(50))
	print(check(-5))
	print(check(200))
	print(check(0))
	print(check(99))
	return 0
}
