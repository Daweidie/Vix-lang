struct Vec2
{
	x: i32
	y: i32
}

fn main(): i32
{
	let a = Vec2 { x: 1, y: 2 }
	let b = Vec2 { x: 3, y: 4 }
	print(a.x + b.x)
	print(a.y + b.y)
	let c = Vec2 { x: a.x * b.x, y: a.y * b.y }
	print(c.x)
	print(c.y)
	return 0
}
