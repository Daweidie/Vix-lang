fn main(): i32 {
    let n = 100
    let mut sum_sq = 0
    let mut sum = 0
    for (i in 1 .. n + 1) {
        sum = sum + i
        sum_sq = sum_sq + i * i
    }
    let sq_sum = sum * sum
    print(sq_sum - sum_sq)
    return 0
}
