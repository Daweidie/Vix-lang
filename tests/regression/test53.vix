type Result:[T, E] = Ok(T) | Err(E)

fn main(): i32
{
	let r1: Result:[i32, string] = Ok(42)
	let r2: Result:[i32, string] = Err("fail")

	match r1
	{
		Ok(_) -> print("has-value")
		Err(_) -> print("error")
	}

	match r2
	{
		Ok(_) -> print("has-value")
		Err(_) -> print("error")
	}

	return 0
}
