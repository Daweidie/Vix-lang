type Pair = struct { left: i32, right: i32 }
type Wrap = struct { pair: Pair, tag: string }

fn main(): i32 {
    let w = Wrap{ tag: "edge", pair: Pair{ right: 7, left: 5 } }
    print(w.pair.left)
    print(w.pair.right)
    print(w.tag)
    return 0
}
