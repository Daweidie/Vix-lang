fn reverse(arr: [i32]): [i32] {
    let mut result: [i32] = []
    let mut i = arr.length - 1
    while (i >= 0) {
        result.push(arr[i])
        i = i - 1
    }
    return result
}
fn main(): i32 {
    let arr = [1, 2, 3]
    let r = reverse(arr)
    for (v in r) { print(v) }
    return 0
}