fn fib(n: i32): i32
{
	if (n <= 1) {
		return n
	}
	return fib(n - 1) + fib(n - 2)
}

fn main(): i32
{
	let r1 = fib(fib(3))
	print(r1)
	print(fib(5) + fib(6))
	let x = fib(2)
	let y = fib(3)
	print(x + y)
	return 0
}
