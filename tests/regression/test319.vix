fn map(arr: &[i32], f: fn(i32): i32) {
    for (i in 0 .. arr.length) {
        arr[i] = f(arr[i])
    }
}
fn double(x: i32): i32 { return x * 2 }
fn main(): i32 {
    let mut arr = [1, 2, 3, 4, 5]
    map(&arr, double)
    for (v in arr) { print(v) }
    return 0
}
