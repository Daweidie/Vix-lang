type Counter = struct { count: i32 }
fn increment(c: &Counter) { c.count = c.count + 1 }
fn main(): i32 {
    let mut c = Counter{ count: 0 }
    increment(&c)
    increment(&c)
    increment(&c)
    print(c.count)
    return 0
}
