struct Rect {
	width: f64,
	height: f64
}

fn area(r: Rect): f64
{
	return r.width * r.height
}

fn main(): i32
{
	let r = Rect{ width: 3.0, height: 4.0 }
	print(area(r))
	let r2 = Rect{ width: 5.0, height: 6.0 }
	print(area(r2))
	return 0
}
