type Point = struct {
    x: i32
    y: i32
}

fn get_x(p: &Point): i32
{
    return p.x
}

fn get_y(p: &Point): i32
{
    return p.y
}

fn main(): i32
{
    let pt = Point{ x: 10, y: 20 }
    let px = get_x(&pt)
    let py = get_y(&pt)
    print(px)
    print(py)
    return 0
}
