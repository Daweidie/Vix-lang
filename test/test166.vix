fn sum_range(start: i32, end_: i32) -> i32
{
	let sum = 0
	for (i in start .. end_) {
		sum = sum + i
	}
	return sum
}

fn main(): i32
{
	print(sum_range(0, 10))
	print(sum_range(1, 11))
	print(sum_range(5, 10))
	return 0
}
