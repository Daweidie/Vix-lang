type Color = Red | Green | Blue

fn main(): i32
{
	let c = Red
	let d = Blue
	match c {
		Red -> { print("red") }
		Green -> { print("green") }
		Blue -> { print("blue") }
	}
	match d {
		Red -> { print("red") }
		Green -> { print("green") }
		Blue -> { print("blue") }
	}
	return 0
}
