struct Point { x: i32, y: i32 }
struct Line { start: Point, end_: Point }

fn main(): i32
{
	let l = Line {
		start: Point { x: 0, y: 0 },
		end_: Point { x: 10, y: 20 }
	}
	print(l.start.x)
	print(l.start.y)
	print(l.end_.x)
	print(l.end_.y)
	return 0
}
