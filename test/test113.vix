fn double(x: i32) -> i32
{
	return x * 2
}

fn triple(x: i32) -> i32
{
	return x * 3
}

fn apply(f: fn(i32): i32, x: i32) -> i32
{
	return f(x)
}

fn main(): i32
{
	let f = fn(n: i32): i32 { n + 1 }
	print(apply(double, 5))
	print(apply(triple, 5))
	print(f(10))
	return 0
}
