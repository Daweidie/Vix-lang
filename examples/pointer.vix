// 指针示例：取地址 + 解引用 + 数组指针运算
fn main(): i32 {
    // 取地址 + 解引用读
    let x = 10
    let ptr = ref x
    let value = @ptr
    print(value)

    // 数组指针 + 偏移
    let arr = [1, 2, 3, 4, 5]
    let p = ref arr[0]
    let second = @(p + 1)
    print(second)
    return 0
}
